# Build/test environment: runtime deps + build/test extras.
-r requirements-runtime.txt
pybind11>=2.10
nanobind>=2.9,<3.0
pytest>=7.0
# Pulled in transitively by ttnn at build time.
pandas<3
loguru>=0.6.0
graphviz
seaborn>=0.13.2
