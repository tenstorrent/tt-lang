name: Build tt-lang

on:
    workflow_dispatch:
        inputs:
            build_docs:
                description: "Build and deploy documentation."
                type: boolean
                default: false
    workflow_call:
        inputs:
            build_docs:
                description: "Build and deploy documentation."
                type: boolean
                required: false
                default: false

permissions:
    checks: write
    contents: read

jobs:
    configure-deps:
        name: "Configure dependencies"
        runs-on: ubuntu-22.04
        timeout-minutes: 120

        defaults:
            run:
                shell: bash

        env:
            CMAKE_BUILD_PARALLEL_LEVEL: 2

        steps:
            - name: Maximize space
              uses: tenstorrent/tt-github-actions/.github/actions/maximize_space@main

            - name: Checkout tt-lang
              uses: actions/checkout@v4
              with:
                  submodules: recursive
                  fetch-depth: 0

            - name: Install system dependencies
              run: |
                  sudo apt-get update
                  sudo apt-get install -y \
                      build-essential \
                      libhwloc-dev libyaml-cpp-dev libboost-all-dev \
                      libtbb-dev libcapstone-dev \
                      cmake ninja-build ccache pkg-config \
                      python3-venv python3-dev \
                      wget zstd

            - name: Install Clang 17
              run: |
                  wget https://apt.llvm.org/llvm.sh
                  chmod u+x llvm.sh
                  sudo ./llvm.sh 17
                  sudo apt-get install -y libc++-17-dev libc++abi-17-dev
                  sudo ln -sf /usr/bin/clang-17 /usr/bin/clang
                  sudo ln -sf /usr/bin/clang++-17 /usr/bin/clang++
                  rm -f llvm.sh

            - name: Extract submodule SHAs for cache keys
              id: submodule-shas
              run: |
                  echo "llvm-sha=$(git -C third-party/llvm-project rev-parse HEAD)" >> $GITHUB_OUTPUT
                  echo "ttmetal-sha=$(git -C third-party/tt-metal rev-parse HEAD)" >> $GITHUB_OUTPUT

            - name: Restore LLVM cache
              id: cache-llvm
              uses: actions/cache/restore@v4
              with:
                  path: build/llvm-install/
                  key: ${{ runner.os }}-llvm-install-${{ steps.submodule-shas.outputs.llvm-sha }}

            - name: Restore tt-metal cache
              id: cache-ttmetal
              uses: actions/cache/restore@v4
              with:
                  path: third-party/tt-metal/build/
                  key: ${{ runner.os }}-ttmetal-build-${{ steps.submodule-shas.outputs.ttmetal-sha }}-${{ hashFiles('third-party/patches/ttmetal-*.patch') }}

            - name: Configure (builds LLVM + tt-metal on cache miss)
              run: |
                  cmake -G Ninja -B build \
                      -DCMAKE_BUILD_TYPE=Release \
                      -DTTLANG_ENABLE_PERF_TRACE=ON

            - name: Save LLVM cache
              if: steps.cache-llvm.outputs.cache-hit != 'true'
              uses: actions/cache/save@v4
              with:
                  path: build/llvm-install/
                  key: ${{ runner.os }}-llvm-install-${{ steps.submodule-shas.outputs.llvm-sha }}

            - name: Save tt-metal cache
              if: steps.cache-ttmetal.outputs.cache-hit != 'true'
              uses: actions/cache/save@v4
              with:
                  path: third-party/tt-metal/build/
                  key: ${{ runner.os }}-ttmetal-build-${{ steps.submodule-shas.outputs.ttmetal-sha }}-${{ hashFiles('third-party/patches/ttmetal-*.patch') }}

    build-tt-lang:
        name: "Build and test tt-lang"
        needs: configure-deps
        runs-on: ubuntu-22.04
        timeout-minutes: 60

        defaults:
            run:
                shell: bash

        steps:
            - name: Maximize space
              uses: tenstorrent/tt-github-actions/.github/actions/maximize_space@main

            - name: Checkout tt-lang
              uses: actions/checkout@v4
              with:
                  submodules: recursive
                  fetch-depth: 0

            - name: Install system dependencies
              run: |
                  sudo apt-get update
                  sudo apt-get install -y \
                      build-essential \
                      libhwloc-dev libyaml-cpp-dev libboost-all-dev \
                      libtbb-dev libcapstone-dev \
                      cmake ninja-build ccache pkg-config \
                      python3-venv python3-dev \
                      wget zstd

            - name: Install Clang 17
              run: |
                  wget https://apt.llvm.org/llvm.sh
                  chmod u+x llvm.sh
                  sudo ./llvm.sh 17
                  sudo apt-get install -y libc++-17-dev libc++abi-17-dev
                  sudo ln -sf /usr/bin/clang-17 /usr/bin/clang
                  sudo ln -sf /usr/bin/clang++-17 /usr/bin/clang++
                  rm -f llvm.sh

            - name: Extract submodule SHAs for cache keys
              id: submodule-shas
              run: |
                  echo "llvm-sha=$(git -C third-party/llvm-project rev-parse HEAD)" >> $GITHUB_OUTPUT
                  echo "ttmetal-sha=$(git -C third-party/tt-metal rev-parse HEAD)" >> $GITHUB_OUTPUT

            - name: Restore LLVM cache
              id: cache-llvm
              uses: actions/cache/restore@v4
              with:
                  path: build/llvm-install/
                  key: ${{ runner.os }}-llvm-install-${{ steps.submodule-shas.outputs.llvm-sha }}
                  fail-on-cache-miss: true

            - name: Restore tt-metal cache
              id: cache-ttmetal
              uses: actions/cache/restore@v4
              with:
                  path: third-party/tt-metal/build/
                  key: ${{ runner.os }}-ttmetal-build-${{ steps.submodule-shas.outputs.ttmetal-sha }}-${{ hashFiles('third-party/patches/ttmetal-*.patch') }}
                  fail-on-cache-miss: true

            - name: Configure tt-lang
              run: |
                  cmake -G Ninja -B build \
                      -DCMAKE_BUILD_TYPE=Release \
                      -DTTLANG_ENABLE_PERF_TRACE=ON

            - name: Setup ccache
              uses: hendrikmuhs/ccache-action@v1.2
              with:
                  key: ${{ runner.os }}-ttlang-build
                  max-size: 200M

            - name: Build tt-lang
              run: |
                  source build/env/activate
                  cmake --build build

            - name: Smoketest
              run: |
                  source build/env/activate
                  python test/python/smoketest.py

            - name: Run ttlang Lit MLIR tests
              run: |
                  source build/env/activate
                  cmake --build build --target check-ttlang-mlir

            - name: Run ttlang Python binprdings tests
              run: |
                  source build/env/activate
                  cmake --build build --target check-ttlang-python-bindings

            - name: Python lit tests
              run: |
                  source build/env/activate
                  cmake --build build --target check-ttlang-python-lit

            - name: Build documentation
              if: inputs.build_docs
              run: |
                  source build/env/activate
                  cmake --build build --target ttlang-docs

            - name: Upload documentation artifact
              if: inputs.build_docs
              uses: actions/upload-pages-artifact@v3
              with:
                  path: ./build/docs/sphinx/_build/html

            - name: Archive build artifacts
              run: |
                  set -x
                  ARTIFACT_NAME=$(.github/scripts/get-artifact-archive-name.sh "${{ github.sha }}")
                  echo "ARTIFACT_NAME=${ARTIFACT_NAME}" >> $GITHUB_ENV

                  tar -I 'zstd --long=31 --adapt=min=9 -T0' -cf "${ARTIFACT_NAME}" \
                      --exclude='build/llvm-build' \
                      build/ third-party/tt-metal/build/

            - name: Upload build artifacts for hardware tests
              uses: actions/upload-artifact@v4
              with:
                  name: ttlang-build-ubuntu
                  path: ${{ env.ARTIFACT_NAME }}
                  if-no-files-found: error
                  retention-days: 1

    test-hardware:
        needs: build-tt-lang
        if: always() && needs.build-tt-lang.result == 'success'
        uses: ./.github/workflows/call-test-hardware.yml
        secrets: inherit
        with:
            runs-on: n150
            build-os: ubuntu
            timeout: 60
