# Runtime dependencies for the tt-lang wheel; also read by pyproject.toml.
pydantic<3
torch>=1.9.0
numpy>=1.20.0
greenlet>=3.0.0
PyYAML>=5.4.0,<=6.0.1
typing_extensions>=4.12.2
ml_dtypes>=0.1.0,<=0.6.0; python_version<'3.13'
ml_dtypes>=0.5.0,<=0.6.0; python_version>='3.13'
# Note: ttnn is *not* listed here because it would force every base install
# (including sim-only / macOS / dev environments and CI sim tests) to pull
# the ~hundreds-of-MB ttnn wheel, and importing real ttnn on a runner with
# no hardware or sfpi crashes the process. ttnn is pinned in
# pyproject.toml's `[project.optional-dependencies] device` extras and
# verified by .github/scripts/check-tt-metal-pin.sh against
# third-party/tt-metal-version.
